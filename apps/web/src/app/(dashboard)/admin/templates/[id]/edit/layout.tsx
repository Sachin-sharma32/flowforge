export const metadata = {
  title: 'Edit Template - FlowForge',
  description: 'Edit global workflow template',
};

export default function EditTemplateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
