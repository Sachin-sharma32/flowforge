export const metadata = {
  title: 'Create Template - FlowForge',
  description: 'Create global workflow templates',
};

export default function NewTemplateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
